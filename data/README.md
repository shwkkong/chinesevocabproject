# Data directory

- `raw/` holds downloaded source datasets (not committed).
- `derived/` holds generated artifacts like the SQLite dictionary DB (not committed by default).

Run the scripts in `scripts/` to build the bundled SQLite database.
